def est_touvhé():
    # definit si lors d'un tirs si le tir touche ou pas avec une certaine valeur dans la grille
      par exemple avec des 0;-1;1. Si la fonction return 1 le tour du joueur reste le meme et inversement

def touche_coulé():
    # renvoie un message au joueur si le tirs touche ou pas un bateau si aucun bateaux est touché la case
      change de couleur pour montrer que cette case a etait touché

def verification():
    # le fonction verifie si les 5 bateau sont bien placés et que aucun bateau ne depasse la grille si
      la verification est faite le jeu peux commencer

def commencement():
    # la fonction decide avec un random.randint qui peux jouer le premier tour entre l'ordinateur et le joueur


le jeu est une bataille navale qui a pour but de couler tout les bateaux des adversaires avant que l'adversaire
     coule votre bateau

regles du jeu

A propos: qui fait quoi dans le projet

la liste de version avec explications

des objectifs de date 

cahier de test